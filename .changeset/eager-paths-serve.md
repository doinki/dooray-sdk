---
'@dooray-sdk/mcp': patch
---

Note in the `task_create` and `task_update` tool descriptions that projects may require tags. Point to `project_tag_list` so agents include any required tags and avoid creation/update failures.
