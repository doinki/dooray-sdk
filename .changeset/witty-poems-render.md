---
'@dooray-sdk/mcp': minor
---

Add a `dooray-mcp://docs/body-syntax` resource documenting Dooray-flavored Markdown: task links and member mentions (`dooray://` URIs), inline images, callouts, collapsibles, column layouts, `:datetime`, and `uml`/`mermaid`/`chart`/`embed` code fences. Every body-accepting tool's `body` description and the server instructions point to it.
